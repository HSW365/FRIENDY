{
  "name": "friendy-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Friendy iOS app shell (Capacitor) \u2014 wraps the Friendy web experience with native StoreKit/RevenueCat purchases and App Store compliance features.",
  "scripts": {
    "build": "node scripts/build.js",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "postcap": "bash scripts/postcap.sh"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/status-bar": "^6.0.1",
    "@revenuecat/purchases-capacitor": "^9.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.23.1"
  }
}
